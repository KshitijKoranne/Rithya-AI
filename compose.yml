services:
  little-lamp:
    build: .
    restart: unless-stopped
    ports:
      - "${HOST_PORT:-3000}:3000"
    environment:
      PORT: 3000
      SARVAM_API_KEY: ${SARVAM_API_KEY}
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 512M
